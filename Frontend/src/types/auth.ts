export interface DemoAccount {
  name: string;
  role: string;
}

export interface Department {
  id: number;
  label: string;
}

export interface AuthLanguage {
  code: string;
  flag: string;
  label: string;
}
